# Dashboard

Frontend app for Robin Wood. Add your preferred stack (e.g. Vite + React, Next.js) here.

## Scripts

- `bun run dev` – start dev server (configure in package.json)
- `bun run build` – build for production
